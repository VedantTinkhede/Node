// Define a variable 'amount' with a value of 9
const amount = 9;

// Check if 'amount' is less than 10 and log a message accordingly
if (amount < 10) {
    console.log('small number');
} else {
    console.log('large number');
}

// Log a simple message to the console
console.log(`hey its my first node app`);